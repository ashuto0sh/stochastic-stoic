---
import BaseHead, { type ArticleMeta } from "../components/BaseHead.astro";
import ThemeToggle from "../components/ThemeToggle.astro";

interface Props {
  title: string;
  description: string;
  ogImage?: string | undefined;
  article?: ArticleMeta | undefined;
}

const { title, description, ogImage, article } = Astro.props;
---

<html lang="en">
  <head>
    <BaseHead
      title={title}
      description={description}
      ogImage={ogImage}
      article={article}
    />
    <slot name="head" />
  </head>
  <body>
    <header class="site-header">
      <nav aria-label="Main">
        <a href="/" class="site-title">Stochastic Stoic</a>
        <div class="nav-right">
          <ul class="nav-links">
            <li>
              <a
                href="/"
                aria-current={
                  !Astro.url.pathname.startsWith("/notes") &&
                  !Astro.url.pathname.startsWith("/about") &&
                  !Astro.url.pathname.startsWith("/search")
                    ? "page"
                    : undefined
                }>Posts</a
              >
            </li>
            <li>
              <a
                href="/notes/"
                aria-current={
                  Astro.url.pathname.startsWith("/notes") ? "page" : undefined
                }>Notes</a
              >
            </li>
            <li>
              <a
                href="/about/"
                aria-current={
                  Astro.url.pathname.startsWith("/about") ? "page" : undefined
                }>About</a
              >
            </li>
            <li>
              <a
                href="/search/"
                aria-current={
                  Astro.url.pathname.startsWith("/search") ? "page" : undefined
                }>Search</a
              >
            </li>
          </ul>
          <ThemeToggle />
        </div>
      </nav>
    </header>
    <main>
      <slot />
    </main>
    <footer class="site-footer">
      <p>
        &copy; {new Date().getFullYear()} Stochastic Stoic &middot;{" "}
        <a href="/rss.xml">RSS</a>
      </p>
    </footer>
  </body>
</html>
